import {StyleSheet} from 'react-native'

const DashStyle = StyleSheet.create({
    container:{
        marginVertical: 50,
        marginHorizontal: 20,
    },
    title:{
        fontSize: 20,
        marginTop: 40,
        fontFamily:'SegoeUI'
    },

})

export default DashStyle;