import {StyleSheet} from 'react-native'

const DashStyle = StyleSheet.create({
    container:{
        marginVertical: 50,
        marginHorizontal: 20,
    },
    title:{
        fontSize: 25,
        marginTop: 20,
        fontFamily:'SegoeUI'
    },

})

export default DashStyle;